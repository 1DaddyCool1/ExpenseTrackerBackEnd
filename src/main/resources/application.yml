server:
  port: 8080 # Set the port

spring:
  profiles:
    active: db
  datasource:
    url: jdbc:postgresql://ep-spring-waterfall-a2wj0k6l-pooler.eu-central-1.aws.neon.tech/neondb?user=neondb_owner&password=npg_VYropPOkf3x1&sslmode=require
    #jdbc:h2:mem:expensedb;DB_CLOSE_DELAY=-1 #URL for H2 database in memory mode
    driver-class-name: org.postgresql.Driver # JDBC driver for H2
    username: neondb_owner
    password: o
  h2:
    console:
      enabled: true

  jpa:
    hibernate:
      ddl-auto: update #Automatically creates or
                #updates the database schema based on entity classes
    show-sql: true # Enables logging of SQL queries generated by JPA
    database-platform: org.hibernate.dialect.PostgreSQLDialect